from controller import WeatherAppController

if __name__ == '__main__':
    # location
    #
    # weather_data = get_weather_data(latitude, longitude, 'metric')
    # print(weather_data)
    # App(weather_data=weather_data, city=city, country=country)
    WeatherAppController(city='saint etienne')
